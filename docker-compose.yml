services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: visapolicy
      POSTGRES_PASSWORD: visapolicy
      POSTGRES_DB: visapolicy
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
